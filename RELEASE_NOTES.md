# 3.8.0 — 2026-09-07

实际文件启动记录；按阶段检查；干净/高亮版文字、链接和媒体一致性；双版本渲染哈希；按任务配图；区分 AI 复核与真实人工批准。

## 使用

从 START_HERE.md 开始，旧仓库地址和调用名称保持不变。GitHub 更新不等于已安装目录自动更新；将实际运行目录同步至此版本，再核对启动回执中的版本。
旧交付包保留历史状态。重新使用时建立新任务记录，复用有证据的内容，补齐缺少的阶段与文件检查；不自动将旧结果标为 Final。

## 验证与边界

增加完整通过的合成测试包和缺失文件、跳步、陈旧证据、正文丢失、错误链接等失败用例。测试不包含真实 SERP 调研、真实生成图片或真实外部发布；内容判断和视觉检查仍须实际执行并如实署名。
命令：python -m unittest discover -s tests -v。
